{
  "name": "simple-grpc-rouer",
  "version": "1.0.0",
  "description": "A simple grpc router",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "watch": "jest --watchAll",
    "start": "ts-node src/index.ts",
    "docker.build": "docker build -t simple-grpc-rouer:latest .",
    "docker.run": "docker run -it -d -p 50051:50051 --env-file .env simple-grpc-rouer:latest"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@types/js-yaml": "^4.0.5",
    "dotenv": "^16.0.3",
    "js-yaml": "^4.1.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.3"
  },
  "devDependencies": {
    "@types/jest": "^28.1.6",
    "jest": "^28.1.3",
    "ts-jest": "^28.0.8"
  }
}
